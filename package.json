{
  "name": "druk-rate-analysis",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -rf assets && mkdir -p assets && cp -R dist/assets/. assets/ && cp dist/index.html index.html && cp dist/404.html 404.html",
    "preview": "vite preview"
  },
  "dependencies": {
    "file-saver": "^2.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "xlsx": "^0.18.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.10"
  }
}
